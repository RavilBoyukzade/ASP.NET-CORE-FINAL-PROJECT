@model IEnumerable<AboutCardViewModel>

@foreach (var item in Model)
	{
		  <div class="grid">
                <div class="icon">
                    <i class="@item.Icon"></i>
                </div>
                <h3>@item.Name</h3>
                <p>@item.Text</p>
            </div>
	}
    